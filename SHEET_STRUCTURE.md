# 공부합시당 캠스터디 — 스프레드시트 구조 문서

Google Sheets ID: `1jjIo-SulFyonrv2dSFYO4SVsejKgbfogCJLVLbA-0Ao`
서비스 계정: `id-630@crack-decorator-450006-g9.iam.gserviceaccount.com`
(키 파일 경로: `study_sw/resource/sheetAccessKey.json` — 이 문서와 별개로 보관, git에 커밋 금지)

이 문서는 2026-08-18에 서비스 계정 권한으로 시트 원본(수식 포함)을 직접 조회해 작성했다.
탭 종류: `template`, `1`~`15`(개인 탭, 구조 동일), `집계`, `권한관리`, `제보상점`.
아래는 개인 탭 1개(`1`번, 재희)를 기준으로 전체 구조를 기록한 것 — 모든 개인 탭이 동일한 셀 배치를 공유한다.

## 개인 탭 (1~15, template) 구조

### 상단 메타 정보 (2~3행)

| 셀 | 내용 | 비고 |
|---|---|---|
| B2 | `📝 {이름} 님의 집계표 📝` | 집계 탭 C열이 `MID(B2, 3, ...)`로 이름만 파싱하므로 앞 이모지+공백 2글자가 정확해야 함 |
| I2 | 가입일자 (`YYYY-MM-DD`) | |
| I3 | `=IF(TODAY()<I2, "D-"&..., "D+"&...)` | D+n 표시, 백엔드 `ROW_ACCESSION_DDAY`(row 2, col I=8) |
| L3 | 참여상태 (`스터디장`/`부스터디장`/`스터디원`/빈값) | 빈값이면 "빈 시트"로 취급됨(집계 O열 수식) |
| O3 | 목표시간 (`8H (교시제)`/`9H (교시제)`/`10H (교시제)`/`8H (달성제)`/`9H (달성제)`/`10H (달성제)`) | `LEFT(O3,2)`로 8H/9H/10 판별 |
| R3 | 예치금 재납 상태 (`미납`/`납부`/빈값) | |
| U3 | 예치금 반환 예상 (수식, 아래 참고) | |

**U3 수식 (예치금 반환 예상액)**:
```
=IFS(
    L3="", "₩0",
    IFERROR(VALUE(RIGHT(I3,LEN(I3)-FIND("+",I3)))<30, FALSE), "₩0 (30일 미만)",
    C33=1, "₩0 (벌금 미납)",
    R3="미납", "₩0 (예치금 미납)",
    R3="납부", "₩0 (예치금 재납)",
    SUM(C39,C40,C42)>=2, "₩0 (송출 P " & SUM(C39,C40) & "회 / 주간 P " & C42 & "회)",
    SUM(C39,C40,C42)>=1, "₩5,000 (송출 P " & SUM(C39,C40) & "회 / 주간 P " & C42 & "회)",
    TRUE, "₩10,000 (송출 P " & SUM(C39,C40) & "회 / 주간 P " & C42 & "회)"
)
```
주의: 이 수식은 `C39`(금주 송출 P)+`C40`(누적 송출 P)+`C42`(누적 주간 P)를 더해 페널티 총점을 계산한다.
`frame-checker-worker/index.js`의 `depositRefundBreakdown()`이 이 로직을 그대로 재현하고 있음(검증 완료).

### 요일별 교시 기록 (4~19행)

- 4행: 요일 헤더(월~일), 5행: 서브헤더(시작/종료/참여율)
- 각 요일은 3개 서브컬럼을 가짐: **시작 / 종료 / 참여율**
- 요일 시작열(0-indexed, `STATUS_DAY_COLS`와 동일): **C(2), F(5), I(8), L(11), O(14), R(17), U(20)** = 월/화/수/목/금/토/일
- 6~19행: 1~14교시. 각 행 = `["01".."14"교시], 시작시각, 종료시각, 참여율(%또는 "ERR")]`
  - 참여율 열은 "시작열+2" (`COL_PERIOD_RATE_OFFSET = 2`와 일치)
  - `"ERR"` 값은 시간 파싱 실패/미기록 등을 나타내는 특수 텍스트

### 반휴/기록 메타 (20~23행)

| 행 | 내용 |
|---|---|
| 20 | 😴 일반반휴 (그날 사용 여부/횟수, 요일 시작열 기준) — `ROW_NORMAL_LEAVE_USE=19`(0-idx) |
| 21 | 😴 사유반휴 — `ROW_REASON_LEAVE_USE=20`(0-idx) |
| 22 | ⏰ 출석시간 |
| 23 | 🤖 기록시점 — `ROW_RECORD_TIME=22`(0-idx)와 일치. `isConfirmed(recordRow[col])`가 이 타임스탬프 존재 여부로 그날 기록 확정 여부를 판단 |

### 총계 섹션 (24~34행, 0-idx 23~33)

| 행(1-idx) | 라벨 | 수식 요지 | index.js 상수 |
|---|---|---|---|
| 25 | ⏰ 일간 학습시간 | `TEXT(MAX(0,(로그+가산)-보정시작시각),"[HH]:MM")` | `ROW_DAILY_STUDY_TIME=24`(0-idx) |
| 26 | ⏰ 로그 학습시간 | 원본 로그값(자동 기록) | index.js에 전용 상수 없음 — 25행 수식에서만 참조됨 |
| 27 | ⏰ 가산 학습시간 | 수동 가산분 | `ROW_BONUS_STUDY_TIME=26`(0-idx) |
| 28 | ⏰ 주간 학습시간 | 요일별 시간 합산(HH:MM), U열은 별도 목표시간 역산식 | `M28`이 목표시간 표시(집계 탭에서 참조) |
| 29 | 💰 일간 총 벌금 | `MIN(반휴횟수별 상한(2000/3000), 목표벌금+오전벌금 합)` | `ROW_TOTAL_FINE=28`(0-idx) |
| 30 | 💰 일간 목표시간 벌금 | LET 수식, 목표분/반휴가중치/부족분→500원 단위 CEILING | `ROW_GOAL_FINE=29`(0-idx) |
| 31 | 💰 오전 목표시간 벌금 | 09시 이전 미달성 시 시간당 500원(최대 1500) | `ROW_MORNING_FINE=30`(0-idx) |
| **32** | **✅ 납부확인** | **드롭다운(빈값)** — 아래 상세 참고 | `ROW_PAYMENT_CHECK=31`(0-idx) |
| **33** | **✅ 미납신호** | `=IF(COUNTIF(C32:W32,"미납")>0, 1, 0)` | `ROW_FINE_NO_STATUS=32`(0-idx), C33 |
| 34 | 💰 주간 총 벌금 | `=SUMIF(C32:W32, "납부", C29:W29)` | `ROW_WEEKLY_TOTAL_FINE=33`(0-idx) |

**중요 — "✅ 납부확인" 행(32행) 데이터 유효성 확인 결과**:
요일 시작열(C, F, I, L, O, R, U)마다 `ONE_OF_LIST` 드롭다운이 `strict: true`로 걸려 있으며,
허용값은 정확히 3개: **`"미납"`, `"납부"`, `"면제"`**. 기본값은 빈 문자열(공란)이며, 앱스크립트
(`appscript.js:701-702`)가 `fine_value > 0 && fine_check === ""`일 때만 자동으로 `"미납"`을
채워 넣는다 — 즉 벌금이 발생했는데 아직 아무 상태도 지정 안 된 요일만 자동으로 "미납"이 찍히고,
"납부"/"면제"는 관리자가 수동으로(또는 이번에 추가한 MONEY 탭 버튼으로) 지정해야 하는 값이다.

C34(주간 총 벌금) 수식이 `SUMIF(..., "납부", ...)`라서, **"납부"로 바뀐 요일의 벌금만 실제
징수액(주간 총 벌금)에 합산된다** — "미납"이나 "면제"는 이 합계에서 제외됨. 이는 "주간 총 벌금"이
"이미 걷은 돈"을 의미하고, "면제"는 벌금 자체를 없었던 일로 처리한다는 뜻으로 읽힌다.

### 상점/페널티 섹션 (35~46행)

| 행 | 라벨 | 수식 요지 |
|---|---|---|
| 35 | 🏅 주간 총 상점 | 학습시간상점+제보상점(주중완료시만)을 배율 곱하고, 페널티/벌금 차감. 0점 조건: L3 빈값 / 송출+주간P≥1 / 제보벌점≥3 / 주간총벌금≥5000 / 사유반휴≥3장 |
| 36 | 🏅 학습시간 상점 | 로그학습시간(분) ÷ 60 × 0.1 |
| 37 | 🏅 제보상점 | 스터디장/부스터디장이면 0.5 고정, 아니면 `제보상점!K{row}` 참조 |
| 38 | (제보상점 시트 행 번호) | C38에 숫자만 저장, 라벨 없음 |
| 39 | 🚨 금주 송출 P | 제보상점 L열 값 기준 0/1/2 매핑 |
| 40 | 🚨 누적 송출 P | 수동 관리 숫자 |
| 41 | 🚨 금주 주간 P | 목표시간 미달 + 교시참여율<80 조건 합산(0~1) |
| 42 | 🚨 누적 주간 P | 수동 관리 숫자 |
| 43 | 🚨 교시 참여율 | `(참여율≥85 카운트+ERR카운트)/목표교시수*100`, 교시제만 유효(달성제는 "-") |
| 44 | 😴 일반 반휴 잔여 | `2 - (1회 사용*1 + 2회 사용*2)` |
| 45 | 😴 사유 반휴 잔여 | `4 - (46행 값 + 사유반휴 합계)` |
| 46 | 😴 사유 반휴 사용 | 숫자(용도 불명확, 45행 계산에 사용됨) |

## 집계 탭 구조

- 4~18행 = 회원번호 1~15, 각 행이 `INDIRECT("'"&B{row}&"'!...")`로 해당 개인 탭을 실시간 참조
- B열: 회원번호(숫자), C열: 이름(개인탭 B2 파싱)
- D열: 타이머(달성/목표, `C28 & " / " & M28`)
- F열: 상점(개인탭 C35)
- G열: 순위 — 상점 내림차순, 동점이면 타이머(C28) 내림차순, 1~3위는 메달 이모지, 4~5위는 🏅, 6위 이하는 숫자
- H/I열: 송출 P(금주/누적) = 개인탭 C39/C40
- J/K열: 주간 P(금주/누적) = 개인탭 C41/C42
- L열: 교시 참여율 = 개인탭 C43
- **N열(상태)**: LET 수식 — `L3`(참여상태) 빈값이면 "빈 시트", `O3`(목표시간) 빈값이면 "달성시간 미등록",
  `R3="미납"`(예치금)이고 `C33=1`(벌금미납신호)이면 "예치금 + 벌금 미납", `R3="미납"`만이면 "예치금 미납",
  `C33=1`만이면 "벌금 미납", 둘 다 아니면 `O3`(목표시간) 그대로 표시
- N열: ⏰ 목표시간 변경(매주 월 14:00 마감) — 프론트 `/goal-schedule` GET/POST가 읽고 쓰는 예약 셀이 바로 이 열.
  `M3:P5` 범위로 헤더 순서를 재확인한 결과 M=🔎상태, N=⏰목표시간 변경, O=😴일반반휴 신청이며,
  `handleGetGoalSchedule`/`handleSetGoalSchedule`(index.js)이 쓰는 `집계!N{row}`와 정확히 일치함 — 확인 완료.
- O열: 😴 일반반휴 신청(매일 23:00 마감)
- 20행: `💰 총 모금액` = `D21+D22+D23 + IF(H4+J4>=1, D24, 0)`
  - D21: 💰 이월 상금
  - D22: 💰 주간 벌금 = `SUM(MAP(B4:B18, 각 개인탭!C34))` — 15명의 "주간 총 벌금"(=SUMIF로 "납부" 처리된 것만) 총합
  - D23: 💰 퇴실 벌금
  - D24: 💰 퇴실 예치 (단, H4+J4≥1일 때만 합산 — 1번 회원의 송출P 금주/누적 합이 1 이상인 경우로 보이나 조건의 의미는 불명확, 재확인 필요)

## 권한관리 탭 구조

- 4~18행 = 회원번호 1~15
- B열: 회원번호, C열: 이름(개인탭 B2에서 `FIND("📝 ")`/`FIND(" 님")` 기준 파싱 — 집계 탭과 파싱 방식이 약간 다름(TRIM+MID vs FIND 기반)이나 결과는 동일)
- D열: 이메일
- H열: 준비 시험(자유 텍스트)
- `listAllMembers()`(index.js)가 `권한관리!A1:H50`을 읽어 `{number, name, email}` 목록을 만드는 것과 일치

## 제보상점 탭 구조

- 4행부터 회원번호 1~15
- B열: 회원번호, C열: 이름
- D~J열: 월~일 요일별 제보 점수(숫자, 기본 0)
- K열(총점): `=MIN(SUM(D:J)*0.1, 0.5)` — 최대 0.5점 캡
- L열(벌점): 별도 숫자(자동 계산 아님, 수동/스크립트 입력 추정)
- `getReportScore()`(index.js)가 `제보상점!D{row}:L{row}`를 읽어 `{total: K열, penalty: L열}` 반환하는 것과 일치
  (D~L 상대 인덱스: row[7]=K열 total, row[8]=L열 penalty — 확인 완료)

## 앱스크립트(`study_sw/resource/appscript.js`) 자동화 요지

- `spread_sheet_id = "1jjIo-SulFyonrv2dSFYO4SVsejKgbfogCJLVLbA-0Ao"` (동일 시트)
- `fine_no_status_cell = "C33"`, `fine_check_row = "32"` — index.js 상수와 일치
- 벌금 발생 시(`fine_value > 0`) 해당 요일 시작열 32행이 공란이면 자동으로 `"미납"` 기입 (701-702행)
- 페널티 누적(`total_pen = 금주송출P+누적송출P+누적주간P`)이 2 이상이면 예치금 재납 셀(`deposit_again_cell`, R3로 추정)에 `"미납"` 자동 기입 (720-721행)
- 이외에도 출석시각 보정(missed_period 반영), 반휴 처리 등 다수 로직 포함 — 이번 조사에서는 벌금/미납 관련 부분만 발췌

## 이번에 구현한 "MONEY 탭 벌금 미납 처리" 기능과의 관계

- `frame-checker-worker/src/index.js`의 `listUnpaidFines()`는 15개 개인 탭의 32행(`ROW_PAYMENT_CHECK`)을
  훑어 값이 정확히 `"미납"`인 요일 셀만 추려 프론트에 전달한다.
- `handleAdminFineStatus()`가 `writeSheetValues`로 해당 요일 셀에 `"미납"/"납부"/"면제"` 중 하나를 그대로
  기입한다 — 이 문서에서 확인한 데이터 유효성 규칙과 정확히 일치하는 값이므로 시트 드롭다운과 충돌 없음.
- "납부"로 바뀌면 C34(주간 총 벌금) SUMIF에 해당 요일 벌금이 합산되기 시작하고, C33(미납신호)이 0이 되어
  U3(예치금 반환 예상액) 계산에서 "벌금 미납" 사유가 해제된다. "면제"로 바뀌어도 C33은 0이 되지만
  C34 합산에는 포함되지 않는다(면제된 돈이므로 징수액에서 빠짐) — 의도된 동작으로 보임.
